import { SnippetForm } from "@/features/admin";

const AddPage = () => (
  <main className="flex mx-auto flex-col items-center justify-center px-2 pt-8 pb-8 max-w-[960px]">
    <SnippetForm />
  </main>
);

export default AddPage;
